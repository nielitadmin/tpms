# Deny access to .env and configuration files
<FilesMatch "^\.env|config\.php">
    Order allow,deny
    Deny from all
</FilesMatch>

# Optional: Remove .php extensions from URLs for cleaner routing
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.php [NC,L]